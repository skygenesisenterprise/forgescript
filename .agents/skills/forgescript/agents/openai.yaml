interface:
  display_name: "Forgescript"
  short_description: "Repo-specific patterns and workflows for forgescript"
  default_prompt: "Use the forgescript repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true